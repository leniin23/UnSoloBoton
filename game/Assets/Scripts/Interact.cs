using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Interact : MonoBehaviour
{

    /*provisional para testing*/
    [SerializeField] float distRayoSymmetron;
    private Transform cam;   // Objeto que contiene rotacion posicion y escala
    // Start is called before the first frame update
    void Start()
    {
        cam = transform;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
